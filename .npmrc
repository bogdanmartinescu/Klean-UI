# Use the official NPM registry
registry=https://registry.npmjs.org/

# Save exact versions of dependencies
save-exact=true

# Enable progress bar for installs
progress=true

# Cache settings
cache=~/.npm/cache

# Use a specific directory for global packages
prefix=~/.npm-global
